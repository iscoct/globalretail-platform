# Copy to backend.hcl and fill in YOUR values. The storage_account_name is
# the one bootstrap.ps1 created — look it up in ../../infra/backend.hcl.
#
# Consumed by: terraform init -backend-config=backend.hcl

resource_group_name  = "rg-tfstate-globalretail-weu"
storage_account_name = ""               # copy from ../../infra/backend.hcl
container_name       = "tfstate"
key                  = "cicd/dev/terraform.tfstate"
use_azuread_auth     = true
subscription_id      = ""               # az account show --query id -o tsv
tenant_id            = ""               # az account show --query tenantId -o tsv
