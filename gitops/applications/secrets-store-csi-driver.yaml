# The CSI Secrets Store Driver (the K8s side — the DaemonSet that mounts
# volumes from external secret stores). Provider-agnostic.

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: secrets-store-csi-driver
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  sources:
    - repoURL: https://kubernetes-sigs.github.io/secrets-store-csi-driver/charts
      chart: secrets-store-csi-driver
      targetRevision: 1.6.0
      helm:
        valueFiles:
          - $values/security/secrets-store-csi/values-driver.yaml
        releaseName: secrets-store-csi-driver

    - repoURL: https://github.com/iscoct/globalretail-platform
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: kube-secrets-store-csi-driver

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
      # CSIDriver from this chart includes spec.tokenRequests that triggers
      # ArgoCD's structured-merge-diff to error with
      #   '.spec.serviceAccountTokenInSecrets: field not declared in schema'
      # (the field exists in the K8s OpenAPI but ArgoCD's cached schema
      # diverges). Replace=true uses kubectl-replace semantics for the
      # CSIDriver resource, bypassing the typed-diff entirely.
      - Replace=true
