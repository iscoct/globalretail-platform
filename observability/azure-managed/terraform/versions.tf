terraform {
  required_version = ">= 1.9.0"

  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }

  # Empty backend block — config supplied at init via -backend-config.
  backend "azurerm" {}
}
